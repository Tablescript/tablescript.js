a = 'I also have a ham radio'
double = fn(x) {
  x * 2
}
t = table {
  1: 'first'
  2: 'second'
}

if (system.arguments.length) {
  system.print(system.arguments[0])
  system.print(system.arguments[1])
  system.print(system.arguments[2](3))
}

{
  a: a,
  double: double,
  t: t
}
